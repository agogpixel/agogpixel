import { addProjectToMetadataInTree } from './add-project-to-metadata-in-tree';

describe('addProjectToMetadataInTree', () => {
  it('should exist', () => {
    expect(addProjectToMetadataInTree).toBeDefined();
  });
});
