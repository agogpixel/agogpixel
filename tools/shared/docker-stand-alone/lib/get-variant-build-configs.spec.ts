import { getVariantBuildConfigs } from './get-variant-build-configs';

describe('getVariantBuildConfigs', () => {
  it('should exist', () => {
    expect(getVariantBuildConfigs).toBeDefined();
  });
});
