/**
 * Docker image family.
 */
export enum Family {
  /**
   * Alpine based images.
   */
  Alpine = 'alpine',

  /**
   * Debian based images.
   */
  Debian = 'debian',

  /**
   * Ubuntu based images.
   */
  Ubuntu = 'ubuntu',
};
