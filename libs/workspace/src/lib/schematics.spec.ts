import { createFiles } from './schematics';

describe('schematics', () => {
  describe('createFiles', () => {
    it('should exist', () => {
      expect(createFiles).toBeDefined();
    });
  });
});
